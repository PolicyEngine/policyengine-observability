Google Cloud Logging writes now carry an explicit per-call timeout with retries disabled (default 2 seconds, `OBSERVABILITY_GOOGLE_LOG_TIMEOUT_SECONDS`), replacing the transport defaults that could block a caller for up to 60 seconds per record when the Logging API degrades. The destination also gains `emit_batch`, writing multiple records in one bounded call.
